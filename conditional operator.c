// COnditional Operator
#include<stdio.h>
int main(){
	int a , b , max , min;
	scanf("%d %d", &a , &b);
	printf("Max = %d Min = %d " ,  a > b ? a : b , a < b ? a : b);
//	max = a > b ? a : b ; 
	// a > b => True => max = a
	// a > b => false => max = b
//	min = a < b ? a : b ;
	
	
}
